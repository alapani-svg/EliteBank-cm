// src/environments/environment.prod.ts — production build (Render-hosted API)
// Update apiUrl below with your Render service URL after the first deploy,
// then re-deploy Vercel.

export const environment = {
  production: true,
  apiUrl: 'https://elite-bank-api.onrender.com',
};
